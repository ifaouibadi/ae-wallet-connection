import { hooksWalletCommunicationWalletBeacon } from './hooks-wallet-communication-wallet-beacon';

describe('hooksWalletCommunicationWalletBeacon', () => {
  it('should work', () => {
    expect(hooksWalletCommunicationWalletBeacon()).toEqual(
      'hooks-wallet-communication-wallet-beacon'
    );
  });
});
